name: Publish

on:
  push:
    branches: [ dblp ]

jobs:
  sbom:
    uses: mdblp/.github/.github/workflows/sbom.yml@main
    with:
      app-name: data
      main-path: ./services/data
    secrets: inherit